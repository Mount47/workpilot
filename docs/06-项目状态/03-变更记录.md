# 文档与架构变更记录

## 2026-07-23

- 项目进入面向校招的功能冻结与收尾阶段，定位统一为“证据驱动、可验证、具备可靠执行基础的项目分析 Agent”；
- 明确当前是 production-oriented prototype，不宣称已经完成自动恢复、鉴权、多租户、可靠队列和生产监控；
- 新增跨平台离线 `scripts/demo.py`：使用仓库 Fixture 与 Stub 完成六步 Run，并自动验收 Evidence、引用、Verification、Trace 和核心 Artifact；
- 新增 Demo 端到端契约测试，防止一键演示随主链路演进失效；
- 修复 Windows 子进程输出代码页与 UTF-8 强制解码冲突，Demo 改为字节捕获并仅在失败时容错解码；
- 重写 README 的首屏定位、三分钟演示、核心工程点、已验证能力和限制；
- 新增 `07-求职展示`，提供 30 秒/3 分钟介绍、简历表述、证据映射、压力追问、风险信号和五分钟演示清单；
- HA-02D、HA-03、P1/P2 保留为未来路线，不再作为本轮校招收尾门槛；
- Windows/Python 3.12 无测试数据库回归为 299 passed、6 skipped；离线 Demo、compileall 和 React/Vite 生产构建通过。

## 2026-07-22

- 完成 HA-02C Run Lease/Fencing：`runs` 新增 owner、expiry 和单调 `execution_attempt`，数据库时间与行锁作为租约权威；
- 新增 `20260722_0004` migration 与 `(state, lease_expires_at)` 索引，真实 PostgreSQL 完成带 Checkpoint 数据的 `0003 → 0004 → 0003 → 0004` 验收；
- Run claim、renew、assert 和 terminal commit 均为原子事务；过期接管进入 recovering 并递增 attempt；
- PlanStep、ToolCall、Checkpoint、Artifact Metadata 和 Run 终态写入全部校验 owner + attempt + expiry，旧 Worker fail closed；
- 新增 LeaseHeartbeat；Runtime 在步骤开始、工具返回和 Checkpoint 提交前检查所有权，失租/Repository 错误立即中止 Scheduler；
- API 后台 Worker 改为 claim → Heartbeat → Runtime → fenced terminal commit，owner token 不进入 Trace，仅记录哈希指纹前缀；
- 新增 `WORKPILOT_LEASE_TTL_SECONDS=30` 与 `WORKPILOT_HEARTBEAT_SECONDS=10`，配置强制 Heartbeat 不超过 TTL 三分之一；
- 真实 PostgreSQL 双 Worker 验证并发 claim 唯一赢家、过期接管、旧 Worker 全写入隔离和新 Worker Checkpoint/终态提交；
- Windows/Python 3.12 + PostgreSQL 17 完整回归 304 passed，Python compileall 与 React/Vite 生产构建通过；无数据库环境为 298 passed、6 skipped；
- 完成 HA-02B Versioned Checkpoint：六个受限工具使用显式 JSON Codec，未知工具/版本/对象 fail closed，不使用 pickle；
- 新增 `20260722_0003` migration、Checkpoint Metadata、Run/PlanStep checkpoint sequence；
- Checkpoint 文件采用规范 JSON、同目录临时文件、flush/fsync、原子 replace、读回、SHA-256、大小、路径和身份校验；
- ToolCall、PlanStep、Checkpoint Metadata 和 Run sequence 在同一 Repository 事务提交，支持同内容幂等和连续序号约束；
- 新增内部 CheckpointRestorer，重建 Plan、StepResultStore、EvidenceStore、ProjectSnapshot、渲染产物、验证结果、预算和 WorkingMemory；
- 新增六步骤 DB-commit/进程退出模糊窗口、文件/数据库提交失败、损坏 Checkpoint、语义不一致和并发 sequence 竞争回归；
- PostgreSQL 17.6 带 HA-02A 旧数据 upgrade/downgrade/re-upgrade、并发提交与完整回归通过，当前为 281 passed；
- 完成 HA-02A Durable Execution Ledger：统一 API/Runtime Run ID，新增 PlanStep、ToolCall 与 Artifact Metadata 持久化；
- 新增 `20260722_0002` migration，并验证带 HA-01 旧 Run 数据的 upgrade、downgrade、再次 upgrade；
- API 新增 `Idempotency-Key`，同 Key 同请求不重复启动，参数冲突返回 409，原始 Key 不入库；
- Runtime 新增 ExecutionObserver，ToolCall 与 PlanStep 完成/失败在一个 Repository 事务内更新，blocked/skipped 也可持久化；
- ArtifactWriter 改为同目录临时文件、fsync 与原子 replace，Metadata 从落盘实际字节计算；
- PostgreSQL 17.6 并发幂等、API 重建执行账本和 Artifact Metadata E2E 通过；完整回归 246 passed；
- 启动 HA-01 PostgreSQL Repository 与 Run 持久化；
- 新增稳定 `RunRecord` / `RunRepository` 协议、线程安全 InMemory 实现和 SQLAlchemy Adapter；
- 新增 Alembic 配置与 `20260722_0001` migration，创建 `runs` 表及状态/时间索引；
- FastAPI Run 创建、状态更新、列表、详情、报告、Artifact 与源文件定位全部改为 Repository 驱动；
- 数据库不可用时 fail closed，不静默回退内存；内存模式必须显式 `--in-memory-runs`；
- 新增 `WORKPILOT_DATABASE_URL` SecretStr 配置与 `[database]` 可选依赖；数据库凭证不接受命令行传递；
- 新增 Repository、SQLAlchemy、migration、API 重建和 CLI 失败语义测试；
- 修复 Windows 测试以系统 GBK 读取 UTF-8 Artifact 的跨平台问题；
- 新增真实 PostgreSQL API 验收：关闭旧 Repository、重建 App 后仍可查询历史 Run 与报告；
- 使用隔离 PostgreSQL 17.6 完成空库 upgrade、downgrade、再次 upgrade 和 Repository/API 重建 E2E；
- HA-01 阶段基线为 Windows/Python 3.12 + PostgreSQL 17.6 完整回归 218 passed，PostgreSQL 离线 migration DDL 与 React/Vite 生产构建通过。

## 2026-07-21

- 新增 FastAPI 服务层（`src/workpilot/api/`）：触发 Run、轮询状态、读取报告/证据/验证产物与源文件端点；
- Run 在后台线程执行，工作区与 Provider 均走白名单，拒绝路径穿越与未授权模型调用；
- 新增 `workpilot serve` CLI 命令与 `[web]` 可选依赖组（fastapi + uvicorn）；
- 新增 React 前端（`web/`，Vite + TS）：渲染周报，[E-xxxx] 引用可点击跳转原文并高亮引用行，展示证据门控验证结果；
- Evidence Store 落盘 evidence.json；修复 EvidenceStore.export() 遗留 datetime 导致 JSON 序列化失败（改用 model_dump(mode="json")）；
- 修复 create_run 将产物写入硬编码路径 `workspace_root/_api_runs` 而非配置的 runs_root 的缺陷——该缺陷导致每次测试都向仓库写入 Run 产物（累积约 290 个文件）；现将 runs_root 贯通至路由，测试写入 tmp_path；
- .gitignore 补充 node_modules、web/dist、tests/fixtures/_api_runs；
- 当前测试结果更新为 203 passed（含 12 个 API 测试）；
- 已知遗留：Run 注册表仍为内存态，服务重启丢历史；前后端为开发态双进程；API 未接入鉴权，仅靠 localhost 绑定与白名单兜底。详见交接文档《06-项目状态/04-交接与难点》。

## 2026-07-15

- BC-005 qwen-plus Golden Run 最终 passed，1 次业务修订恢复；
- 5 次物理调用、14782 Token、99.23 秒，最终 68 项验证零错误；
- Evidence、Claim、Citation、Entity Field Support 和实体 Recall 均为 100%；
- Owner P/R 100%，Due Date Recall 50%，Mitigation Recall 0%；
- BC-012、BC-013 标记已回归，剩余字段召回问题登记 BC-014；
- Golden 改为穷举 4 个 ActionItem、2 个 Risk，并修复 Stub 复合句分类；
- 当前测试结果更新为 176 passed；
- 扁平 Schema qwen-plus 回归成功构建实体，字段 Evidence Support 100%；
- 回归发现 15 个 explicit Claim 全部正文漂移，登记 BC-012；
- explicit Claim 改由 primary Evidence quote 提供事实文本；
- 修订发现 RiskStatus 缺少 blocked，登记并修复 BC-013；
- 当前测试结果更新为 174 passed；
- BC-005 qwen-plus Golden 回归因嵌套 SupportedText 的 `model_type` 失败；
- 失败 Run 使用 9443 Token、耗时 62.61 秒，Evidence 仍为 100%；
- 登记 BC-011，并将模型 Draft 改为扁平 value/refs；
- 领域层继续使用严格 SupportedText，不降低字段 Evidence 约束；
- 当前测试结果更新为 171 passed；
- 完成 BC-005 结构化实体字段 Golden Schema 与独立 Suite；
- 新增实体 Detection 和五类字段的 TP/FP/FN、Precision/Recall；
- 明确未标注字段与显式 null 的不同评测语义；
- 登记并修复 BC-010：Stub 使用 Markdown 章节优先分类行动项；
- Stub 的 Action/Risk Entity Recall 均达到 100%，字段 Recall 按设计为 0%；
- 当前测试结果更新为 169 passed；
- BC-005 第二次 qwen-plus 回归成功构建结构化实体，但 Run 后续修订失败；
- 实测 Action owner/due date 填充率为 100%/33.33%，Risk owner/severity/mitigation 为 66.67%/0%/0%；
- 登记并修复 BC-008：确定性还原 explicit Claim 缺失的单个列表 marker；
- 登记并修复 BC-009：修订异常时仍持久化最后一次完整验证报告；
- Structured Repair 增加代码白名单静态校验消息，保持输入值不进入反馈；
- 当前测试结果更新为 164 passed；
- BC-005 首次真实回归在 Claim Schema 阶段失败，登记 BC-006 与 BC-007；
- 两次盲重试合计消耗 6450 Token，未进入 Artifact 和字段评测；
- Structured Retry 增加脱敏字段路径与错误类型反馈；
- ProviderResponseError 移除 Raw 模型响应，防止正文进入 Trace/Eval；
- 当前测试结果更新为 160 passed；
- 完成 BC-005 第一阶段结构化领域改造；
- ActionItem/Risk 进入 ProjectSnapshot，Milestone 建立预留形状；
- ClaimDraft 增加受限字段 Draft，复用 Analysis 模型调用；
- 新增 EntityFieldVerifier，字段虚构和越权引用会 fail closed；
- Artifact Schema 升级到 0.3 并输出 field_evidence；
- Eval 新增实体字段支持率和五项字段填充率；
- 当前测试结果更新为 157 passed；
- 完成 BC-002 qwen-plus 真实回归，15/15 Candidate 接受且无需模型 Repair；
- Evidence Precision/Recall、Acceptance 和两类 Source Coverage 均为 100%；
- Run 经 1 次 Claim 修订后 passed，总 Token 6581、耗时 47.38 秒；
- Locator Repair Count 为 0，确认本轮主要收益来自行号视图 Prompt；
- 登记 BC-004 Markdown 双 marker 并完成展示层修复；
- 登记 BC-005 风险与行动项业务字段缺失，留待正式领域模型解决；
- 当前测试结果更新为 148 passed；
- BC-002 第一版修复完成；
- exact quote 存在但行号错误时由代码确定性重定位；
- 重复 quote 选择距离模型建议行号最近的位置；
- OpenAI-compatible 与 Claude Evidence Prompt 增加行号视图；
- Repair Prompt 增加脱敏失败原因计数；
- Trace 与 Eval 增加 Locator Repair Count 和丢弃原因分布；
- 当前测试结果更新为 145 passed，总体覆盖率约 90%。

## 2026-07-14

- 完成 BC-001 相同 qwen-plus 真实回归；
- 门禁触发一次 Evidence Repair，额外 862 Token，Recovery 0%，Run 正确 fail closed；
- BC-001 false-pass 已回归验证，Repair 重复失败登记为 BC-002；
- 发现零验证检查被计为 100% 并登记 BC-003；
- 验证类空指标改为 null/N/A，Suite 聚合忽略不适用 Case；
- 当前测试结果更新为 144 passed；
- 建立独立 Bad Case 目录、记录规范、模板和状态流转要求；
- 将首次 qwen-plus 来源遗漏登记为 BC-001；
- Eval Runner 新增来源覆盖、Evidence 接受/丢弃、Claim 来源覆盖指标；
- Eval Runner 新增 Evidence Repair 触发率、恢复率、额外调用、Token 和成本指标；
- Repair 成本只统计修复窗口，未知价格保持 null；
- CLI Eval 输出关键证据质量指标；
- 当前测试结果更新为 143 passed；
- 新增每来源 SourceExtractionReport；
- 新增 Source Coverage 与 Evidence Acceptance 确定性门禁；
- 候选全部丢弃、Provider 错误或全局零 Evidence 时，只对问题来源执行一次有界修复；
- 新增 Evidence 来源到 Claim 的 SourceCoverageVerifier；
- 新增证据门禁和修复 Trace，保持正文不进入日志；
- 新增规则单测和首次失败、第二次恢复的 Runtime 端到端测试；
- 当前测试结果更新为 140 passed；
- 完成百炼 qwen-plus 首次真实模型 Run；
- 记录 4 次物理模型调用、3682 Token、1 次修订和最终 passed；
- 识别 13 个 Evidence Candidate 仅接受 3 个、一个来源零覆盖的问题；
- 明确当前 passed 证明引用有效性，不证明信息覆盖完整；
- 新增真实运行问题复盘和 Agent Loop 三类重试实现文档；
- 修正运行时状态机中已经过时的 Planner 与 Scheduler 描述。

## 2026-07-13

- 完成 Provider API Key 严格厂商隔离，移除兼容 Adapter 的跨厂商环境变量猜测；
- 缺 Key、Model、Base URL 和不安全 URL 在 SDK Client 创建前拒绝；
- 新增 ProviderPreflightResult 和 `workpilot doctor`；
- Doctor 支持单 Provider 与 Route Config 全目标离线检查；
- Doctor 检查路由能力要求，且不创建 Client、不发送网络请求、不显示 Key；
- 当前测试结果更新为 131 passed、总体覆盖率约 90%；
- 完成自然语言 success_criteria 与系统 success_rule_ids 双轨模型；
- 完成 SuccessRuleRegistry 和六类核心工具安全摘要规则；
- PlanDraft 不暴露规则 ID，ConstrainedLLMPlanner 只补入 ToolSpec 规则；
- ToolSpec 未知规则和 PlanStep 规则不匹配均在执行前拒绝；
- 规则失败产生 success_criteria_failed，并按 DAG 语义阻塞后继；
- verification.run 的业务 error 与工具执行失败保持分离；
- success_evaluation 已进入 plan.json、Working Memory 和 Trace；
- 当前测试结果更新为 121 passed、总体覆盖率约 90%；
- PlanStepStatus 增加 blocked 和 skipped；
- 完成 StepResultStore 与安全结果摘要；
- 完成 SerialDAGScheduler、稳定拓扑顺序、失败传播和死锁检测；
- Scheduler 支持在业务验证门前暂停并在最终化前恢复；
- Runtime 初始六步主链路改由 Plan dependencies 驱动；
- 修订仍只重入 claims.build、artifacts.render 和 verification.run；
- Scheduler Trace 增加 ready、blocked、paused、deadlocked 和 completed 事件；
- 步骤异常在失败传播后仍保留原始异常类型和失败原因；
- 当前测试结果更新为 115 passed、总体覆盖率约 90%。

## 2026-07-12

- 完成 ToolInput、ToolHandler、CallableToolHandler 和 ToolResult；
- Tool Registry 统一管理 Spec、版本、Input Model 和 Handler；
- PlanValidator 在执行前校验 PlanStep.inputs 并拒绝额外字段；
- Runtime 六个核心工具全部通过 Registry.invoke 执行；
- 新增 tool_call_started、tool_call_completed 和 tool_call_failed 安全事件；
- ToolResult 内部 output 不进入 Trace，只记录 output_summary；
- 当前测试结果更新为 107 passed、总体覆盖率约 90%。
- 完成 PlannerEvalCase、PlannerEvalRunner 和 `workpilot planner-eval`；
- 新增合法计划、未知工具、Provider 超时、非法响应和认证失败离线基线；
- 输出决策准确率、计划合法率、LLM 接受率、降级率、失败率、危险误接受率、Token 和延迟；
- 当前 Planner 基线为 Decision Accuracy 100%、Plan Validity 80%、Fallback 60%、Unsafe Acceptance 0%；仅代表控制策略回归；
- 当前测试结果更新为 101 passed、总体覆盖率约 89%。
- 完成 PlanDraft，模型无法覆盖 Goal、Plan ID 和执行状态；
- 完成 ConstrainedLLMPlanner、RuntimePlanPolicy 和 FallbackPlanner；
- `planning` ModelRoute 已接入 Runtime，未配置时保持 DeterministicPlanner；
- 非法工具、错误映射、缺步、依赖循环和非法响应均可安全降级；
- 认证、预算和未知编程错误禁止被确定性降级掩盖；
- 新增 planner_decision、Planning Token 和 Prompt/Schema 版本 Trace；
- 新增受限 Planner 安全及 Runtime 集成测试；
- 当前测试结果更新为 97 passed、总体覆盖率约 89%。
- 完成预算感知 RetryPolicy、指数退避和稳定 RetryEvent；
- 完成 TaskType、ModelTier、CapabilityRequirement 和 ModelRoute；
- 完成瞬时错误重试耗尽后的显式 Failover，认证失败禁止静默切换；
- 完成 TaskRoutedProvider，并将 Evidence、Analysis 和 Revision 路由接入 Runtime；
- 模型 Trace 增加任务类型、Prompt/Schema 版本、路由目标与路由决策事件；
- 新增版本化 JSON Route Config、CLI `--route-config` 和不含密钥的档位示例；
- OpenAI、Claude、DeepSeek 默认模型标识更新为当前稳定主力型号；
- 新增重试、能力拒绝、最低档位、主备切换、配置安全和 Runtime 路由集成测试；
- 当前测试结果更新为 87 passed、总体覆盖率约 89%。

## 2026-07-11

- 删除旧版 `docs` 内容并从零重建设计文档；
- 将产品定位明确为企业项目协作的证据驱动型 Agent；
- 明确后端 Runtime、Evidence、Verifier、Memory、Evaluation 与高可用的模块边界；
- 建立分类开发清单和统一完成状态；
- 为当前已实现模块补充实现细节；
- 为未来 Planner、Memory、Evaluation、Storage、Connector 和高可用模块建立空白知识入口；
- 建立工程问题与追加式项目点设计机制。
- 建立 SourceLocator、正式 Evidence、Claim 和 ProjectSnapshot 基础模型；
- 将 EvidenceExtractor 与 EvidenceStore 迁移到正式 Evidence 模型；
- 新增领域模型测试，当前测试结果更新为 29 passed、总体覆盖率约 71%。
- 完成 ClaimBuilder、ProjectSnapshot 驱动的确定性 Artifact 渲染；
- 完成 Claim-Support Verifier，并与 Citation Verifier 共同接入修订闭环；
- 新增 `project_snapshot.json` 和 `claims_built` Trace 事件；
- 完成 EvalCase、EvalRunner、`workpilot eval` 和最小 Stub 基线；
- 当前测试结果更新为 41 passed、总体覆盖率约 77%。
- 统一 OpenAI-compatible、Claude 和 Stub Provider 的可观测调用结果；
- 完成 ExecutionBudget，实际执行步骤、时间和 Token 限制；
- Trace 增加步骤层级、模型调用、延迟、Token、请求信息和预算摘要；
- 新增预算失败和 Claude Provider 测试；
- 当前测试结果更新为 46 passed、总体覆盖率约 83%。
- 完成 Run 级 WorkingMemory 与安全 WorkingMemorySnapshot；
- EvidenceStore、ProjectSnapshot、验证反馈、Artifact、步骤和 Budget 已接入 RunContext；
- 成功与预算失败均输出不包含业务正文的 `run_context.json`；
- 新增 Memory 生命周期、安全导出和 Runtime 集成测试；
- 当前测试结果更新为 50 passed、总体覆盖率约 84%。
- 完成 Plan、PlanStep、ToolSpec、Tool Registry 和 PlanValidator；
- 完成 DeterministicPlanner 与实际驱动 Runtime 的 PlanExecutor；
- 完成受控 PlanStep 重入、计划 Trace、Working Memory Plan 摘要和 `plan.json`；
- 新增验证失败后仅重入 Claim/Artifact/Verify 步骤的集成测试；
- 当前测试结果更新为 56 passed、总体覆盖率约 86%。
- 完成数据驱动 Provider Descriptor、Capabilities 和 Transport；
- 新增 Gemini 与 Custom 企业兼容网关支持；
- 完成 Provider 错误分类和 retryable 语义；
- 新增 `workpilot providers` 能力发现命令；
- 新增 Registry、Gemini、Custom 和跨厂商错误契约测试；
- 当前测试结果更新为 71 passed、总体覆盖率约 88%。
