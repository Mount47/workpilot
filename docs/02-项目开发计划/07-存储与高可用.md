# 存储与高可用开发计划

## 持久化

- 🚧 Artifact 和 Trace 写入本地文件；
- 🚧 EvidenceStore 在 Run 内仍为内存实现，但 passed Run 已可导出 `evidence.json`；缺少失败保底导出、可查询 Repository、索引和事务；
- ✅ 已建立 PostgreSQL 数据模型和四版 Alembic 迁移，并通过 PostgreSQL 17 upgrade/downgrade、旧数据保留与 API/Repository 重建验收；
- ✅ Run 身份、状态、失败原因、output_dir、PlanStep、ToolCall 和 Artifact Metadata 已接入执行账本；
- 🚧 Evidence/Claim 可查询索引、Verification 索引和 Budget Ledger 待实现；
- ✅ `RunRepository` 接口隔离存储实现，并提供线程安全 InMemory 与 SQLAlchemy Adapter；
- ✅ API Run 创建、状态更新、列表、详情和 Artifact 定位已切换到 Repository；
- ⬜ 现有 `runs_root` Artifact 一次性索引导入；
- 🚧 普通 Artifact 已原子写入并登记摘要；Checkpoint 已实现文件先提交、数据库后提交和摘要对账，通用 Artifact 对账/清理仍待实现；
- ✅ ToolCall、PlanStep、Checkpoint Metadata 和 Run sequence 在同一数据库事务内完成或回滚。

## 恢复与幂等

- ✅ 六个受限工具已实现版本化 JSON Checkpoint Codec，不使用 pickle；
- ✅ 每个提交步骤生成规范 JSON、SHA-256 和大小校验的原子 Checkpoint 文件；
- ✅ 内部 CheckpointRestorer 可重建 Plan、StepResultStore、Evidence、ProjectSnapshot、验证结果、预算和 WorkingMemory；
- ✅ API Run 创建支持 `Idempotency-Key`，同 Key 同参数不重复启动；参数冲突返回 409；
- 🚧 ToolCall 已有稳定逻辑调用键和输入哈希，Checkpoint 内部重建已实现，但 Provider 侧调用去重未实现；
- ⬜ Provider 请求指纹、调用状态和用量持久化，恢复时避免重复调用；
- 🚧 崩溃边界、内部恢复重建和租约过期接管已验证；启动扫描及 API/CLI 恢复入口未实现；
- ⬜ 部分 Artifact 清理与恢复；
- ⬜ 重复任务检测。

## 并发与可用性

- 🚧 Run 级并发互斥已完成；Project/Tenant 级并发配额未实现；
- ✅ PostgreSQL 行锁 + 数据库时间租约、单调 `execution_attempt` fencing、Heartbeat 和过期接管；
- ✅ PlanStep、ToolCall、Checkpoint、Artifact Metadata 和 Run 终态写入均在事务内校验当前 fence；
- ⬜ 后台任务队列；
- ⬜ 限流与背压；
- ⬜ 健康检查；
- ⬜ 服务级指标和告警；
- 🚧 多实例租约竞争与旧 Worker 隔离已验证；自动发现、调度和恢复过期 Run 未实现。

## 鉴权与隔离

- ⬜ Tenant/User/Project 身份与权限模型；
- ⬜ API、Repository、Artifact、Trace 和 Provider Credential 全链路租户隔离；
- ⬜ per-user/per-tenant Token、成本、并发和速率预算；
- ⬜ 越权读取、取消、恢复和路径访问测试；
- ⬜ 不含敏感正文的审计日志。

## 工程交付

- ⬜ lint 和 type check；
- 🚧 本地 304 项测试在真实 PostgreSQL 17 环境全部通过；无数据库环境为 298 passed、6 项 PostgreSQL 条件测试跳过；CI 分类待实现；
- ⬜ Docker 一键运行；
- ⬜ Prometheus 指标、liveness/readiness/startup 检查和优雅关闭；
- ⬜ 版本化模型价格表、精确成本和脱敏 Trace 持久化；
- ⬜ 示例企业项目 workspace；
- ⬜ 配置、密钥和环境说明；
- ⬜ 备份与数据迁移说明。
