# 规划与工具开发计划

## Planner

- ✅ Plan、PlanStep、PlanStepStatus 与 ToolSpec 模型；
- ✅ DeterministicPlanner 生成当前可信主链路；
- ✅ PlanDraft 与 Provider 结构化计划接口；
- ✅ ConstrainedLLMPlanner 只接收模型可控的步骤字段；
- ✅ RuntimePlanPolicy 固定当前六步 Handler 边界；
- ✅ 非法计划和瞬时 Provider 失败回退 DeterministicPlanner；
- ✅ 认证和预算失败禁止静默回退；
- ✅ 计划 Schema 校验；
- ✅ 依赖存在、循环和重复 ID 检查；
- ✅ Registry、Contract 白名单与最低步骤预算预检查；
- ⬜ 信息不足时追加步骤；
- ⬜ 计划版本和调整原因。

## Plan Executor

- 🚧 PlanExecutor 执行显式步骤并检查依赖，自动 DAG 调度待实现；
- ✅ 步骤输入由 Registry 中的 Pydantic Schema 校验；
- ⬜ 成功标准检查；
- 🚧 失败状态和受控重入已实现，跳过和取消待实现；
- 🚧 reentrant attempt 已实现，幂等键待实现；
- ✅ Plan 状态进入 Working Memory；
- ✅ Plan 创建、验证、步骤 attempt 和失败 Trace。
- ✅ planner_decision、Planning Token 和 Prompt/Schema 版本 Trace；

## Tool Registry

- ✅ ToolSpec、Input Model 和 Handler 统一注册；
- ✅ 统一 ToolHandler、CallableToolHandler 和 ToolResult；
- ✅ 参数 Schema 默认 extra=forbid；
- ✅ 工具名称和版本；
- ✅ Runtime 六个核心工具通过 Registry.invoke 执行；
- ✅ ToolResult 内部 output 与安全 output_summary 分离；
- ⬜ 权限、超时和输出大小限制；
- 🚧 Handler 异常进入稳定失败链路，独立工具错误码待完善；
- ✅ tool_call started/completed/failed 调用审计。

## 定向修订

- 🚧 当前验证错误只重入 claims.build、artifacts.render 和 verification.run 三个 PlanStep；
- ⬜ 标准化验证错误；
- ⬜ 错误到修复动作映射；
- ⬜ 只重做受影响 Claim 或 Artifact；
- ⬜ 补充检索、删除无依据结论和保留冲突策略；
- ⬜ 修订前后差异记录。
